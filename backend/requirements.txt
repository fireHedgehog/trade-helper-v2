# Convenience mirror of pyproject.toml [project.dependencies] for plain
# `pip install -r requirements.txt` workflows. Keep in sync with pyproject.toml.
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
keyring>=25.2
pydantic>=2.7
pydantic-settings>=2.3

# dev
pytest>=8.2
anyio>=4.4
